This server is property of <%= node["motd"]["company"] %>
<% if node["pci"]["in_scope"] -%>
	This server is in-scope for PCI compilance <%= node["platform"] %>
<% else -%>
	This server is not in scope for PCI compilance
<% end -%>